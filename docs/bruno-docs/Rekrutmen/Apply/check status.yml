info:
  name: check status
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/rekrutmen/applied
  headers:
    - name: Authorization
      value: "{{token.mahasiswa1}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
